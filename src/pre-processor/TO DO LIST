-need to do more thorough testing of the pre-processor func before shipping.
-need to run the pre-processor on the virtual machine to verify that it works correctly.
-need to separate the data struct of macro from the getters and setters.
-need to update function documentation
