#ifndef __FIRST_PASS_H
#define __FIRST_PASS_H

#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>
#include <string.h>

typedef enum opcodes opcodes;
typedef struct encoded_instruction encoded_instruction;
#endif